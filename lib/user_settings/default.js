const { BFX_HF_CUSTOM } = require('./charts')
const { DARK } = require('./themes')

module.exports = {
  affiliateCode: 'ohURdMjbH',
  dms: true,
  ga: false,
  chart: BFX_HF_CUSTOM,
  theme: DARK,
}
